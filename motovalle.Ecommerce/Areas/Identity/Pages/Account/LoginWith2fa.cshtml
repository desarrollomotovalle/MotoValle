@page
@model LoginWith2faModel
@{
    ViewData["Title"] = "Two-factor authentication";
}

<section class="b-pageHeader identity">
    <div class="container">
        <h1 class="wow zoomInLeft" data-wow-delay="0.5s">Autenticación en dos pasos</h1>
        <div class="b-pageHeader__search wow zoomInRight" data-wow-delay="0.5s">
            <h3>Llena la información correspondiente</h3>
        </div>
    </div>
</section>

<div class="b-breadCumbs s-shadow wow zoomInUp" data-wow-delay="0.5s">
    <div class="container">
        <a asp-controller="Home" asp-action="Index" class="b-breadCumbs__page">Home</a><span class="fa fa-angle-right"></span><a asp-area="Identity" asp-page="/Account/LoginWith2fa" class="b-breadCumbs__page m-active">Autenticación en dos pasos</a>
    </div>
</div>


<section class="b-breadCumbs wow zoomInUp" data-wow-delay="0.5s">
    <div class="container">
        <div class="row">
            <h2>Autenticación en dos pasos</h2>
            <hr />
            <p>Su inicio de sesión está protegido con una aplicación de autenticación. Ingrese su código de autenticación a continuación.</p>
            <div class="row">
                <div class="col-md-4">
                    <form method="post" asp-route-returnUrl="@Model.ReturnUrl">
                        <input asp-for="RememberMe" type="hidden" />
                        <div asp-validation-summary="All" class="text-danger"></div>
                        <div class="form-group">
                            <label asp-for="Input.TwoFactorCode"></label>
                            <input asp-for="Input.TwoFactorCode" class="form-control" autocomplete="off" />
                            <span asp-validation-for="Input.TwoFactorCode" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <div class="checkbox">
                                <label asp-for="Input.RememberMachine">
                                    <input asp-for="Input.RememberMachine" />
                                    @Html.DisplayNameFor(m => m.Input.RememberMachine)
                                </label>
                            </div>
                        </div>
                        <div class="form-group">
                            <button type="submit" class="btn btn-default">Iniciar sesión</button>
                        </div>
                    </form>
                </div>
            </div>
            <p>
                ¿No tiene acceso a su dispositivo autenticador?. Puedes recuperar tu código para iniciar sesión dando clic 
                <a asp-page="./LoginWithRecoveryCode" asp-route-returnUrl="@Model.ReturnUrl"><b>Aquí</b></a>.
            </p>
        </div>
        @{ await Html.RenderPartialAsync("_ContactUs"); }
    </div>
    <br />
    <br />
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
